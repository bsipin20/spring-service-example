
CREATE TABLE customers (
                           id SERIAL PRIMARY KEY,
                           name VARCHAR(255) NOT NULL
);

CREATE TABLE brackets (
                          id SERIAL PRIMARY KEY,
                          name VARCHAR(255) NOT NULL
);